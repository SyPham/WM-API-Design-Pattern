using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace WM.WebApi.helpers
{
    public class EFExtension<T>
    { 
    //    public List<T> GetAllTaskDescendants(List<T> entity)
    //    {
    //        return List<T>
    //    }
    //    private IEnumerable<T> GetAllDescendants(IEnumerable<T> rootNodes)
    //    {
    //        var descendants = rootNodes.SelectMany(x => GetAllDescendants(x.children));
    //        return rootNodes.Concat(descendants);
    //    }
    }
}
